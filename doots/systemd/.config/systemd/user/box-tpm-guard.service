[Unit]
Description=Warn when the disk fell back to the recovery passphrase
After=graphical-session.target

[Service]
Type=oneshot
Environment=PATH=%h/.local/bin/box-scripts:/usr/local/bin:/usr/bin:/bin
ExecStart=%h/.local/bin/box-scripts/box-tpm-status --notify

[Install]
WantedBy=default.target
