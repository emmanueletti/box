#!/usr/bin/env bash
#
# Opens a zellij session named after the current directory, on the dev layout.

set -euo pipefail

[[ ${1:-} == -h ]] && exec box-help "$0"

zellij options \
  --session-name "$(basename "$PWD")" \
  --attach-to-session true \
  --default-layout dev
