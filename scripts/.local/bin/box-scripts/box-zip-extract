#!/usr/bin/env bash
#
# Extracts one or more archives into the current directory, format auto-detected.
#
#   box-zip-extract thing.tar.zst other.7z

set -euo pipefail

[[ ${1:-} == -h ]] && exec box-help "$0"

for archive in "$@"; do
  bsdtar -xf "$archive"
done
